"""
Detector-scoped fixtures.

Add here when a fixture is only used under ``tests/pkg/detectors/`` (e.g. a mocked
classifier head, per-detector default thresholds). Cross-detector tensors and
``Features`` bundles stay in ``tests/conftest.py``.
"""
